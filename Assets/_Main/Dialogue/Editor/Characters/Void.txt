label VoidRoute "Into the Rabbit Hole"

SetBackgroundImage(Field-Back, 1), SetBackgroundImage(Field-Front)
PlayMusic(night)

m.SetPosition(x=-0.5), eg.SetPosition(x=1.5)

v.SetPosition(0.5, 0.5, speed=5)
v "I knew you would choose me."

if ($v.affinity > 0)
{
	v.SetSprite(yukata-body, body)
	v (flustered) "Let's head deeper into the rabbit hole together."
}
else
{
	v (annoyed) "But I don't choose you."
	jump CommonEnding
}

end VoidRoute